
-- 1. Исследуйте выборку пользователей в таблице
-- Сколько всего пользователей в нашей базе? 3078
-- Есть ли пользователи, которые регистрировались больше одного раза? Нет
-- Если да, то сколько их и по сколько раз они регистрировались? -

-- 2. Исследуйте время регистрации пользователей
-- Какое минимальное и максимальное время регистрации в нашей витрине? Максимальное 30 марта 2023, минимальное 1 июля 2022.
-- Есть ли клиенты с неопределенным временем регистрации? Нет

-- 3. Исследуйте динамику пользовательской базы
-- Растет ли наша пользовательская база с течением времени? Да
-- Какой месяц показал наибольшее количество зарегистрированных пользователей? Март 2023

1.
select    count(*) as cnt_row
        , count(id_user) as cnt_reg
        , count(distinct id_user) as cnt_user 
from skygame.users
-------
select    id_user
		, count(*) as cnt
from skygame.users
group by id_user
having count(*) > 1
order by cnt desc

-------------------- 
2.
select   max(reg_date) as max_date
       , min(reg_date) as min_date
       , sum(case when reg_date is null then 1.0 else 0.0 end) as cnt_null
       , count(reg_date) as cnt_null_2 
from skygame.users

-------------------- 
3.
select    date_trunc('month',reg_date) as mm
		, count(id_user) as cnt
from skygame.users
group by mm
order by mm

select 
   date_trunc ('month', reg_date) as month
   , count (*)
from Skygame.users
group by month
order by month
